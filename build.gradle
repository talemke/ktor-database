plugins {
	id "org.jetbrains.kotlin.jvm" version "$KOTLIN_VERSION"
}

group = "de.talemke"
version = "0.0.1-SNAPSHOT"

repositories {
	mavenCentral()
}

dependencies {

	api "io.ktor:ktor-server-core:$KTOR_VERSION"
	api "org.jetbrains.exposed:exposed-core:0.43.0"

}

